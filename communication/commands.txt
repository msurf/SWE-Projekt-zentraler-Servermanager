befehl -> Beschreibung (unbedingt zu besetzende felder)
immer besetzt sein muss: ID, direction

alles was ein :direct response enthält, wird direkt im ServerSocketThread erledigt und nicht in einem Worker!!
Dies ist vor allem für die Page wichtig, denn dso werden die Daten über den selben Socket zurück geschickt.

Nur für die nicht "direct response" werden Worker erstellt!

die !advanced! machen wir noch nicht, der rest muss da sein. grundsätzlich wird in der Antwort, egal ob direkt oder auf einem anderen Socket, der status = 105(response) und die info mit der antwort gesetzt. Arrays werden in einem String dargestellt, Trennzeichen ist #.

Page-Java

authenticate -> user identifizieren (name, user, password)aus der Datenbank :direct response
getclients -> holt die liste der verfügbaren clients aus der Datenbank :direct response
getclientstatus -> holt den status vom Client :direct response
hwinfo -> holt hardware-infos vom Client (name, client) aus der Datenbank :direct response
swinfo -> holt software-infos vom Client (name, client)	aus der Datenbank :direct response
getrepoliste -> holt die liste der verfügbaren Software(name) aus der DB :direct response


install -> installiert angegebenes Paket (name, client, program)
start -> start dienst (name, client)
stop -> stopt dienst (name, client)
restart -> restarted dienst (name, client)
updatehwinfo -> updated die DB mit hwinfo (name, client)
updateswinfo -> updated die DB mit swinfo (name, client)
updateclientstatus -> updated clientstatus aller Clients in der DB (name)
updaterepoliste -> updated die liste der verfügbaren software in der DB (name)
!advanced! getconfig -> holt die Konfigurationsdatei eines Dienstes (name, client, programm
!advanced! chconfig -> ändert die Konfiguration eines Dienstes(name, client, programm, parameter)

Java-Java

updateclientstatus -> holt status vom client (name, client) : direct response
hwinfo -> holt hardware-infos vom Client (name, client) :direct response
swinfo -> holt software-infos vom Client (name, client)	:direct response
install -> installiert angegebenes Paket(ftpURL) (name, client, ftpURL)
start -> startet dienst (name, D.user, D.password, program, client) //D.user der User aus der DB, der zu dem dienst gehört
stop -> stopt dienst (name, D.user, D.password, program, client)
restart -> restarted dienst (name, D.user, D.password, program, client)
!advanced! getconfig -> holt die Konfigurationsdatei eines Dienstes (name, client, programm
!advanced! chconfig -> ändert die Konfiguration eines Dienstes(name, client, programm, par$


Achtung bei der prüfung auf response(105)!
